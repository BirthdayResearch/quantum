{
  "name": "quantumbridge.app",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "prepare": "husky install",
    "write": "turbo run write",
    "clean": "turbo run clean",
    "build": "turbo run build",
    "playground:start": "turbo run playground:start",
    "build:docker": "turbo run build:docker",
    "test": "turbo run test",
    "test:hardhat": "turbo run test:hardhat",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint -- --fix",
    "format": "prettier --write .",
    "migration:deploy": "turbo run migration:deploy"
  },
  "dependencies": {
    "@waveshq/standard-defichain-jellyfishsdk": "^1.0.5"
  },
  "devDependencies": {
    "@stickyjs/turbo": "^1.1.1",
    "husky": "^8.0.3",
    "lint-staged": "^13.2.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.6.3"
  },
  "packageManager": "pnpm@8.6.3"
}
